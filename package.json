{
  "name": "kyc-triage",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "An onboarding triage agent that decides where it is confident and escalates where it is not.",
  "scripts": {
    "start": "node src/serveur.ts",
    "mesurer": "node src/mesurer.ts",
    "cas": "node src/generer.ts",
    "test": "tsc --noEmit && node src/readme.ts --check && node --test src/*.test.*",
    "typage": "tsc --noEmit",
    "echecs": "node src/echecs.ts",
    "sensibilite": "node src/sensibilite.ts",
    "figures": "node src/readme.ts",
    "bases": "node src/bases.ts",
    "pages": "tsc -p tsconfig.web.json && node src/pages.ts && node src/verifier-ecran.mjs . 1",
    "adverses": "node src/adverses.ts",
    "captures": "node src/capturer.mjs ."
  },
  "engines": {
    "node": ">=24"
  },
  "devDependencies": {
    "typescript": "^5.9.0",
    "@types/node": "^24.0.0"
  }
}
